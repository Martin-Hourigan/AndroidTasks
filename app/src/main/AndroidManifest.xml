<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Requested the first time a reminder is actually set, not at launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      A reminder for 09:00 has to arrive at 09:00; inexact alarms can drift far
      enough to make a "leave now" reminder useless. USE_EXACT_ALARM covers
      Android 13+, SCHEDULE_EXACT_ALARM the versions before it.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Alarms do not survive a reboot; the receiver re-arms them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- The only network this app does: asking GitHub whether a newer build
         exists, and fetching it. Nothing is uploaded and nothing is tracked. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Needed to hand a downloaded APK to the system installer. It does not
         let the app install anything silently — Android still shows its own
         confirmation, and still refuses any APK not signed by the same key. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tasks">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <receiver
            android:name=".notification.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- Both are exempt from the Android 8+ implicit-broadcast limits. -->
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>


        <!-- Where the install session reports back. Not exported: only the
             system's own PendingIntent targets it. -->
        <receiver
            android:name=".update.InstallResultReceiver"
            android:exported="false" />

    </application>

</manifest>
